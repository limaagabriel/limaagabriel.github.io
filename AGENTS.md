# Gabriel Lima — Personal Site

Next.js 16 static export, MDX blog, Tailwind CSS v4, next-intl i18n.

## Code Style

1. **No inline prop types** — define an interface before every component.
2. **One component per file** — never export more than one component from a single file.
3. **No abbreviations** — spell out names fully everywhere.
